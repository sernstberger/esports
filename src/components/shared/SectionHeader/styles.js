import {StyleRules, Theme} from "material-ui/styles";
import variables from "../../../assets/styles/variables";

const styleSheet = (Theme) => ({
  sectionHeader: {
    borderLeft: `7px solid ${variables.primaryColor}`,
  },
});

export default styleSheet;
